json.extract! customer_product_pricelist, :id, :per20ft, :per40ft, :per20od, :per21ft, :per20fr, :per40fr, :ppncategory, :active, :description, :created_at, :updated_at
json.url customer_product_pricelist_url(customer_product_pricelist, format: :json)
