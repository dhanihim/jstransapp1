<h4>Agents</h4>
<br>

<div class="table-responsive">
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Contact</th>
      <th>Type</th>
      <th colspan="3" style="width: 3%;"></th>
    </tr>
  </thead>

  <tbody>
    <% @agents.each do |agent| %>
      <tr>
        <td><%= agent.name %></td>
        <td><%= agent.address %></td>
        <td><%= agent.contact %></td>
        <td><%= agent.pickupordooring %></td>
        <td><%= link_to 'Edit', edit_agent_path(agent), class:"btn btn-info" %></td>
        <td><%= link_to 'X', agent, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>

<%= link_to 'New Agent', new_agent_path, class:"btn btn-primary" %>
