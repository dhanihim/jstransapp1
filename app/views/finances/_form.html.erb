<%= form_with(model: finance) do |form| %>
  <% if finance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(finance.errors.count, "error") %> prohibited this finance from being saved:</h2>

      <ul>
        <% finance.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.text_field :uid, class:'form-control', readonly:'true' %>
  </div>

  <!-- 
  <div class="field form-group">
    <%= form.text_field :total_billing, class:'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.date_select :payment_date, class:'form-control' %>
  </div>

  <div class="field form-group">
    <%= form.text_field :payment_document %>
  </div> -->

  <div class="field form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class:'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit class:'btn btn-primary' %>
  </div>
<% end %>
